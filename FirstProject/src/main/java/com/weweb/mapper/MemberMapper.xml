<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.weweb.mapper.MemberMapper">
	<insert id="signUp" parameterType="com.weweb.command.MemberVO">
		insert into tb_member (email, password, name) values(#{email},#{password},#{name})
	</insert>
	
	<select id="login" resultType="com.weweb.command.MemberVO" parameterType="com.weweb.command.MemberVO">
		select * from tb_member where email = #{email} and password= #{password}
	</select>
	
	<select id="emailCheck" parameterType="String" resultType="int">
		select count(*) as result
		from tb_member
		where email = #{email}
	</select>
	
	<update id="newPassWord" parameterType="com.weweb.command.MemberVO">
		UPDATE tb_member SET password = #{password} WHERE email = #{email}
	</update>
	
	<delete id="deleteMember" parameterType="com.weweb.command.MemberVO">
		delete from tb_member where email = #{email}
	</delete>
</mapper>